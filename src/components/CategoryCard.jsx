const CategoryCard = ({categoria}) => {

    return (
        <div className="category-card">
            <span className="category-icon">{categoria}</span>
        </div>
    )
}

export default CategoryCard